import { hot } from 'react-hot-loader';
import React from 'react';
import './App.scss';

const App = () => {
  return (
    <div className="App">
      Header
      Content
    </div>
  );
};

export default hot(module)(App);
